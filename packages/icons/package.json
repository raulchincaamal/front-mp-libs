{
  "name": "@mp-front/icons",
  "private": false,
  "version": "0.0.2",
  "type": "module",
  "main": "./dist/main.cjs",
  "module": "./dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "dev": "vite",
    "lint": "eslint . --fix",
    "prebuild": "node scripts/update-svgs.js && node scripts/generate-index.js && node scripts/generate-readme.js && npm run lint && git add .",
    "build": "tsc -b ./tsconfig.lib.json && vite build"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "peerDependencies": {
    "react": "19.2.0",
    "react-dom": "19.2.0"
  },
  "dependencies": {
    "react": "19.2.0",
    "react-dom": "19.2.0"
  }
}
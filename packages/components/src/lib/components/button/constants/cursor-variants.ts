import type { CursorType } from "../button.types"

export const cursorStyles: Record<CursorType, string> = {
  pointer: "cursor-pointer",
  default: "cursor-default",
  "not-allowed": "cursor-not-allowed",
  wait: "cursor-wait",
  text: "cursor-text",
}
